'use client'
import { useEffect,useState } from 'react';import { useRouter } from 'next/navigation'
export default function AceitarConvite(){const router=useRouter(),[message,setMessage]=useState('Validando convite…');useEffect(()=>{const token=new URLSearchParams(window.location.search).get('token');if(!token){setMessage('Convite inválido.');return}fetch('/api/usuarios-loja/aceitar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error);setMessage('Convite aceito. Abrindo painel…');setTimeout(()=>router.replace('/dashboard'),600)}).catch(e=>setMessage(e.message))},[router]);return <main className="min-h-screen grid place-items-center p-6"><div className="glass p-8 text-center"><h1 className="text-xl font-semibold">Acesso à equipe</h1><p className="hint mt-3">{message}</p></div></main>}
